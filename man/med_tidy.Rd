% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/med_tidy.R
\name{med_tidy}
\alias{med_tidy}
\title{Extract and tidy mediation analysis results}
\usage{
med_tidy(med_object, exp = FALSE)
}
\arguments{
\item{med_object}{Output from \code{mediation::mediate()}}

\item{exp}{Logical: exponentiate coefficients? Useful for logistic or Cox models (default: FALSE)}
}
\value{
Data frame with columns: estimate, ci_lower, ci_upper, p_value
}
\description{
Converts output from \code{mediation::mediate()} into a tidy data frame
with estimates, confidence intervals, and p-values for ACME, ADE,
Total Effect, and Proportion Mediated.
}
\examples{
\dontrun{
library(mediation)
data(jobs)
m <- lm(job_seek ~ treat, data = jobs)
y <- lm(depress2 ~ treat + job_seek, data = jobs)
med <- mediate(m, y, treat = "treat", mediator = "job_seek", sims = 100)
med_tidy(med)
}
}
